export const ADDON_ID = "tailwind-dark-mode";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `tailwind-dark-mode`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
